spring:
  application:
    name: backend
  datasource:
    # URL kết nối Supabase PostgreSQL
    url: jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require
    username: postgres.hkawigfrbwghzinmhrmx
    password: 63nj5c9EKES3VPUh
    hikari:
      maximum-pool-size: 3
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

logging:
  level:
    root: INFO
    com.tms: DEBUG
  file:
    name: logs/tms-application.log
  logback:
    rollingpolicy:
      max-file-size: 10MB
      max-history: 7
